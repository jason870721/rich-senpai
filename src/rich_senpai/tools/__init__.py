"""Tool surface for the rich-senpai agent.

See ``tools/README.md`` for the SPEC + handler contract. Subpackages are
grouped by concern (file_access, shell, task_board, delegation, messaging,
memory); ``tool_register`` imports them and builds the dispatch table.
"""
